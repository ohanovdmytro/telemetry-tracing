global:
  scrape_interval: 5s

scrape_configs:
  # The app serves OpenTelemetry metrics on the host, outside this network.
  - job_name: telemetry-tracing
    static_configs:
      - targets: ['host.docker.internal:9464']
